# =============================================================================
# Cross-classification visualization: AE: baseline | Features: lat8+dist8 – new data
# Results: results/newdata_validation/baseline/vis_lat8dist8/
# Note: umap_model_pkl is null → vis script fits fresh UMAP on lat8 features
#       (the lat8dist8 UMAP model has 16-dim input, not compatible here).
# =============================================================================
root_folder : "/home/lding/lding/fa_data_analysis"
input:
  latents_csv          : root_folder + "/ae_results/newdata_validation/baseline/latents_newdata.csv"
  fa_type_results_csv  : root_folder + "/ae_results/newdata_validation/baseline/fa_cls_lat8dist8/predictions_all.csv"
  position_results_csv : root_folder + "/ae_results/newdata_validation/baseline/pos_cls_lat8dist8/predictions_all.csv"
  umap_model_pkl       : root_folder + "/ae_results/test_run_overfit_20260322/baseline/fa_cls_lat8/umap_all_model.pkl"
  annotation_csv       : root_folder + "/labelling/project-1-at-2025-12-01-16-46-54a904cb.csv"

output:
  out_dir : root_folder + "/ae_results/newdata_validation/baseline/vis_lat8dist8"

labels:
  fa_type_label_col  : "classification"    # not present in newdata → true-label plots skipped
  position_label_col : "Position"          # not present in newdata → true-label plots skipped
  fa_type_order:
    - "Nascent Adhesion"
    - "focal complex"
    - "focal adhesion"
    - "fibrillar adhesion"
    - "No adhesion"
  position_order:
    - "Cell Protruding Edge"
    - "Cell Periphery/other"
    - "Lamella"
    - "Cell Body"

misc:
  random_state : 42
  log_level    : "INFO"
