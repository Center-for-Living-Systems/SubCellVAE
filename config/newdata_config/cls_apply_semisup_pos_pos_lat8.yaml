# =============================================================================
# Cls Apply: pos-type | AE: semisup_pos | Features: lat8
# Results:   /home/lding/lding/gitcode/SubCellAE/results/newdata_validation/semisup_pos/pos_cls_lat8/
# =============================================================================
root_folder : "/home/lding/lding/fa_data_analysis"
input:
  latents_csv : root_folder + "/ae_results/newdata_validation/semisup_pos/latents_newdata.csv"

model:
  model_pkl      : root_folder + "/ae_results/test_run_overfit_20260322/semisup_pos/pos_cls_lat8/lgbm_model.pkl"
  umap_model_pkl : root_folder + "/ae_results/test_run_overfit_20260322/semisup_pos/pos_cls_lat8/umap_all_model.pkl"

output:
  out_dir : root_folder + "/ae_results/newdata_validation/semisup_pos/pos_cls_lat8"

labels:
  label_order:
    - "Cell Protruding Edge"
    - "Cell Periphery/other"
    - "Lamella"
    - "Cell Body"

features:
  feature_cols : null   # auto-detect z_* columns

dist_features:
  patch_prep_dirs: null
  feature_weight : 20.0

umap:
  n_neighbors  : 15
  min_dist     : 0.1
  random_state : 42

misc:
  log_level : "INFO"
