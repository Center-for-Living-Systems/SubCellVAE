# =============================================================================
# Cross-classification visualization: AE: semisup_pos | Features: lat8+dist8 – new data
# Results: results/newdata_validation/semisup_pos/vis_lat8dist8/
# Note: umap_model_pkl is null → vis script fits fresh UMAP on lat8 features
#       (the lat8dist8 UMAP model has 16-dim input, not compatible here).
# =============================================================================
root_folder : "/home/lding/lding/fa_data_analysis"
input:
  latents_csv          : root_folder + "/ae_results/newdata_validation/semisup_pos/latents_newdata.csv"
  fa_type_results_csv  : root_folder + "/ae_results/newdata_validation/semisup_pos/fa_cls_lat8dist8/predictions_all.csv"
  position_results_csv : root_folder + "/ae_results/newdata_validation/semisup_pos/pos_cls_lat8dist8/predictions_all.csv"
  umap_model_pkl       : null   # fit fresh UMAP on lat8 features

output:
  out_dir : root_folder + "/ae_results/newdata_validation/semisup_pos/vis_lat8dist8"

labels:
  fa_type_label_col  : "classification"    # not present in newdata → true-label plots skipped
  position_label_col : "Position"          # not present in newdata → true-label plots skipped
  fa_type_order:
    - "Nascent Adhesion"
    - "focal complex"
    - "focal adhesion"
    - "fibrillar adhesion"
    - "No adhesion"
  position_order:
    - "Cell Protruding Edge"
    - "Cell Periphery/other"
    - "Lamella"
    - "Cell Body"

misc:
  random_state : 42
  log_level    : "INFO"
