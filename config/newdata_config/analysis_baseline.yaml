# =============================================================================
# Analysis: AE: baseline – new validation data
# Results:  results/newdata_validation/baseline/analysis/
# =============================================================================
root_folder : "/home/lding/lding/fa_data_analysis"
input:
  latents_csv  : root_folder + "/ae_results/newdata_validation/baseline/latents_newdata.csv"
  split_filter : "all"

output:
  out_dir : root_folder + "/ae_results/newdata_validation/baseline/analysis"

embedding:
  methods:
    - UMAP
  umap_model_pkl : root_folder + "/ae_results/test_run_overfit_20260322/baseline/fa_cls_lat8/umap_all_model.pkl"
  # umap_n_neighbors / umap_min_dist ignored when umap_model_pkl is set

clustering:
  kmeans_enabled    : true
  kmeans_n_clusters : 5
  dbscan_enabled    : false
  dbscan_eps        : 0.5
  dbscan_min_samples: 10
  boxplot_kind      : box

label_orders:
  condition_name:
    - "control"
    - "ycomp"

misc:
  log_level : "INFO"
