# =============================================================================
# Cls Apply: pos-type | AE: baseline | Features: lat8dist8
# Results:   /home/lding/lding/gitcode/SubCellAE/results/newdata_validation/baseline/pos_cls_lat8dist8/
# =============================================================================
root_folder : "/home/lding/lding/fa_data_analysis"
input:
  latents_csv : root_folder + "/ae_results/newdata_validation/baseline/latents_newdata.csv"

model:
  model_pkl      : root_folder + "/ae_results/test_run_overfit_20260322/baseline/pos_cls_lat8dist8/lgbm_model.pkl"
  umap_model_pkl : root_folder + "/ae_results/test_run_overfit_20260322/baseline/pos_cls_lat8dist8/umap_all_model.pkl"

output:
  out_dir : root_folder + "/ae_results/newdata_validation/baseline/pos_cls_lat8dist8"

labels:
  label_order:
    - "Cell Protruding Edge"
    - "Cell Periphery/other"
    - "Lamella"
    - "Cell Body"

features:
  feature_cols : null   # auto-detect z_* columns

dist_features:
  patch_prep_dirs:
    - root_folder + "/ae_results/newdata_validation/patches/control/plot_patches32"
    - root_folder + "/ae_results/newdata_validation/patches/ycomp/plot_patches32"
  feature_weight : 20.0

umap:
  n_neighbors  : 15
  min_dist     : 0.1
  random_state : 42

misc:
  log_level : "INFO"
